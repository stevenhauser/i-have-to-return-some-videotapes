import React, { PropTypes } from 'react';

import { createPureComponent } from 'utils/createPureComponent';

export default createPureComponent({

  render() {
    return <p>It is a game.</p>;
  }

});
